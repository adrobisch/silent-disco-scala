require(["angular"], function(angular) {
    describe('Silent Disco Scala server', function() {
        it('should support all client features', function() {
            expect(false).toBe(true);
        });
    });
});
